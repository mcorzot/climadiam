% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/soap_get.R
\name{soap_get}
\alias{soap_get}
\title{Descargar una respuesta SOAP como texto}
\usage{
soap_get(url)
}
\arguments{
\item{url}{\code{character}. Dirección URL completa del servicio SOAP, incluyendo los parámetros
de la consulta (por ejemplo, \code{"http://.../ServicioClima?method=getRedesList&pksesion=1234"}).}
}
\value{
Un vector de tipo \code{character} con el contenido XML completo de la respuesta del servidor,
concatenado en una única cadena de texto.
}
\description{
Función auxiliar que realiza una solicitud HTTP tipo \code{GET} a una URL de servicio web SOAP
y devuelve el contenido de la respuesta como texto sin almacenarlo en disco.

Está pensada para ser utilizada internamente por otras funciones del paquete,
como \code{getwsIDSesion()}, \code{getwsRedesList()}, u otras que acceden a servicios SOAP
de la Junta de Andalucía.
}
\details{
La función utiliza la librería \pkg{curl} (a través de \code{new_handle()} y \code{curl()})
para abrir la conexión HTTP, establece un tiempo máximo de espera de 60 segundos
y lee el contenido línea a línea mediante \code{readLines()}.

En caso de error de conexión o tiempo de espera, se lanza una excepción.
}
\examples{
\dontrun{
# Ejemplo de uso:
xml_text <- soap_get("http://www.juntade.../ServicioClima?method=getRedesList&pksesion=12345")
cat(substr(xml_text, 1, 200))  # Muestra las primeras líneas del XML
}

}
